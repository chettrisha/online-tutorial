angular.module('app').factory('mvCachedCategories', function(mvCategories) {
  var categoriesList;

  return {
    query: function() {
      if(!categoriesList) {
        categoriesList = mvCachedCategories.query();
      }

      return categoriesList;
    }
  }
})